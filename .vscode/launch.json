{
    "configurations": [
        {
            "type": "pwa-chrome",
            "name": "http://${idSection}",
            "request": "launch",
            "url": "http://${idSection}"
        }
    ]
}